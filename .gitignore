# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env — keep real keys out of git; .env.example documents the shape
.env*
!.env.example

# local runtime storage (uploaded media + manifests).
# Only used while STORAGE_DRIVER=local; Supabase replaces this later.
/var/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
next-env.d.ts

# local tooling
/.claude/
